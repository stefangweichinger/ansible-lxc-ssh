---

- name: Store LXD major version
  command:
    shell: echo "::set-output name=LXD_Major_Version::$(lxd --version | cut -f1 -d'.')"
  register: command_output

- debug: msg="{{ command_output }}"
